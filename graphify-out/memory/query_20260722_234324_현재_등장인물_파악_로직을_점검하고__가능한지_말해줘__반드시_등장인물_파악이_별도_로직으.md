---
type: "query"
date: "2026-07-22T23:43:24.284404+00:00"
question: "현재 등장인물 파악 로직을 점검하고, 가능한지 말해줘. 반드시 등장인물 파악이 별도 로직으로 분리될 필요는 없지만, 맥락 파악에는 반드시 등장해야 해. 등장인물이 없으면 없다고 해야 하고"
contributor: "graphify"
outcome: "useful"
source_nodes: ["App()", "participantRoster.ts", "buildCandidatePassBPrompt()", "parseCandidatePassBGeminiAnalysis()", "CandidatePassBVideoFrame", "broadcastContextProtocol.ts", "candidatePassBInsightStore.ts"]
---

# Q: 현재 등장인물 파악 로직을 점검하고, 가능한지 말해줘. 반드시 등장인물 파악이 별도 로직으로 분리될 필요는 없지만, 맥락 파악에는 반드시 등장해야 해. 등장인물이 없으면 없다고 해야 하고

## Answer

Expanded from original query via graph vocab: participant, roster, frame, candidatepassb, context, insight, gemini, qwen. 현재 후보 AI 한 요청이 오디오와 최대 4장 화면을 보고 identifiedParticipants와 사건·반응 맥락을 동시에 생성한다. 특정 방송만 닫힌 roster를 파일명으로 선택하며, 결과는 저장되지만 전체 방송 맥락 입력에는 후보별 인물 판정이 전달되지 않고 빈 배열은 인물 없음과 식별 실패를 구분하지 못한다. 별도 API 호출은 필수가 아니다. 모든 화면 준비를 하드 게이트로 두고 같은 응답에 participantPresence=identified|present-unidentified|none-present, 근거 프레임, 식별 목록을 필수화하며 사건·반응 설명이 이를 참조하도록 검증하면 가능하다.

## Outcome

- Signal: useful

## Source Nodes

- App()
- participantRoster.ts
- buildCandidatePassBPrompt()
- parseCandidatePassBGeminiAnalysis()
- CandidatePassBVideoFrame
- broadcastContextProtocol.ts
- candidatePassBInsightStore.ts